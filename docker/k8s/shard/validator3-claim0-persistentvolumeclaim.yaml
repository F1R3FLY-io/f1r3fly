apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: validator3-claim0
  name: validator3-claim0
  namespace: f1r3fly
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
