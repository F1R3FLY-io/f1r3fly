apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: validator2-claim0
  name: validator2-claim0
  namespace: f1r3fly
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
